import MealPlansPage from '../../components/MealPlansPage';

/**
 * Halaman Rencana Makanan.
 * Merender komponen MealPlansPage.
 *
 * @returns {JSX.Element} Elemen JSX untuk halaman rencana makanan.
 */
export default function MenuPage() {
  return <MealPlansPage />;
}
